spring:
  datasource:
    url: jdbc:postgresql://db:5432/resource_allocation
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
