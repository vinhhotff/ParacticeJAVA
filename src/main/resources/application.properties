# ==========================================
# SHARED CONFIGURATION (COMMON FOR ALL PROFILES)
# ==========================================
spring.application.name=demo

# Active Profile (Loads application-dev.properties by default)
spring.profiles.active=dev

# Server port
server.port=8000

# Swagger Docs Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# JWT Signature Key
jwt.signerKey=z8p4p8X9vY2bN5mR7qW1eT4uI0oP3aS6dF9gH2jK5lZ8x

# Email SMTP Config (Gmail) - Shared config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hk864071@gmail.com
spring.mail.password=ebjiikvdmqxiyple
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Session & Cookie Configurations (Shared Settings)
server.servlet.session.cookie.name=BOOKSTORE_SESSION
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true

# Flyway Baseline Configuration
spring.flyway.baseline-on-migrate=true